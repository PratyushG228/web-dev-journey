# Functions

A function is reusable code.

Example

function greet(name){
    return `Hello ${name}`;
}

Why use them?

- Avoid repetition
- Make code easier to read
- Give one job to one function

Things I learned

- return stops the function
- parameters receive values
- arguments are passed into parameters